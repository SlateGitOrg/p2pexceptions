[project]
name = "p2pexceptions"
version = "0.1.0"
description = "Invoice exception analysis with a MECE cause taxonomy validated against hand labels, so the percentages actually mean something."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
